---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "gpcn_virtualmachine_images Data Source - gpcn"
subcategory: ""
description: |-
  Retrieves available virtual machine images for a given datacenter. Results can be filtered by image type and/or name
---

# gpcn_virtualmachine_images (Data Source)

Retrieves available virtual machine images for a given datacenter. Results can be filtered by image type and/or name

## Example Usage

```terraform
terraform {
  required_providers {
    gpcn = {
      source  = "Global-Private-Cloud-Network/gpcn"
      version = "~>0.5.4"
    }
  }
}

provider "gpcn" {
  host = "https://api.gpcn.com"
}

# Lookup a datacenter first
data "gpcn_datacenters" "central_us" {
  country_name = "United States"
  region_name  = "Central"
  name         = "Chicago"
}

# List all Linux images for a datacenter
data "gpcn_virtualmachine_images" "linux_images" {
  datacenter_id = data.gpcn_datacenters.central_us.datacenters[0].id
  image_type    = "Linux"
}

# Find a specific image by name (substring match, case-insensitive)
data "gpcn_virtualmachine_images" "alma_8" {
  datacenter_id = data.gpcn_datacenters.central_us.datacenters[0].id
  image_name    = "Alma Linux 8"
}

# Use the image ID when creating a virtual machine
resource "gpcn_virtualmachine" "example" {
  name          = "terraform-demo-vm"
  datacenter_id = data.gpcn_datacenters.central_us.datacenters[0].id

  size = {
    category = "general-purpose"
    name     = "G-Micro-1"
  }

  image_id = data.gpcn_virtualmachine_images.alma_8.images[0].id

  allocate_public_ip = false

  initial_auth = {
    username   = "almalinux"
    ssh_key_id = "your-ssh-key-id"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `datacenter_id` (String) The ID of the datacenter to retrieve images for

### Optional

- `image_name` (String) Filter images by name (case-insensitive substring match)
- `image_type` (String) Filter images by type. Must be one of: Linux, Windows, Network/Security, Custom.

### Read-Only

- `images` (Attributes List) List of virtual machine images matching the specified filter criteria (see [below for nested schema](#nestedatt--images))

<a id="nestedatt--images"></a>
### Nested Schema for `images`

Read-Only:

- `id` (String) Unique identifier of the image. Use this as the image_id in a gpcn_virtualmachine resource
- `image_type` (String) The category this image belongs to
- `name` (String) Human-readable name of the image
