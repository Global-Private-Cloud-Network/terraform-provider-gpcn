---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "gpcn_vpc_public_ip Resource - gpcn"
subcategory: ""
description: |-
  Acquires an elastic public IP address from a VPC and holds it. The address stays with the VPC until it is released, so it survives the virtual machines it serves. Attach it to a network interface with gpcn_vpc_public_ip_attachment. GPCN releases an attached address as readily as a held one, so destroying this resource while the address serves a machine takes that machine's connectivity away. Requires the API-key permissions vpc:read, vpc-public-ip:create and vpc-public-ip:delete.
---

# gpcn_vpc_public_ip (Resource)

Acquires an elastic public IP address from a VPC and holds it. The address stays with the VPC until it is released, so it survives the virtual machines it serves. Attach it to a network interface with gpcn_vpc_public_ip_attachment. GPCN releases an attached address as readily as a held one, so destroying this resource while the address serves a machine takes that machine's connectivity away. Requires the API-key permissions vpc:read, vpc-public-ip:create and vpc-public-ip:delete.

## Example Usage

```terraform
# Example: Acquiring a GPCN VPC elastic public IP address

terraform {
  required_providers {
    gpcn = {
      source  = "Global-Private-Cloud-Network/gpcn"
      version = "~>1.4.0"
    }
  }
}

provider "gpcn" {
  host = "https://api.gpcn.com"
}

# The VPC the address is acquired from: a gpcn_vpc resource.
variable "vpc_id" {
  type = string
}

# The address is held by the VPC until an attachment binds it to an interface.
resource "gpcn_vpc_public_ip" "example" {
  vpc_id = var.vpc_id
}

output "public_ip_address" {
  value = gpcn_vpc_public_ip.example.ip_address
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `vpc_id` (String) Unique identifier of the VPC the address is acquired from. Changing this value requires replacing the address

### Read-Only

- `created_time` (String) Timestamp when the address was acquired
- `failure_reason` (String) Why the last operation on the address failed, or null when none did
- `held` (Boolean) Whether the address is held by the VPC rather than serving a virtual machine. It is true while virtual_machine_id is null
- `id` (String) Unique identifier for the public IP address in UUID format
- `ip_address` (String) The address itself. It is null until the platform allocates it, which happens shortly after the acquisition
- `last_updated` (String) Timestamp when the address was last updated
- `state` (String) Lifecycle state of the address: 'acquiring', 'ready', 'failed' or 'removing'
- `virtual_machine_id` (String) Unique identifier of the virtual machine the address serves, or null while the address is held

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# The identifier names the VPC first and then the address: <vpc_id>/<public_ip_id>.
terraform import gpcn_vpc_public_ip.example "c13808d9-3b7d-42c5-a21d-f0961308a38a/ab50b650-ec09-41a8-96c8-979dc5802a16"
```
